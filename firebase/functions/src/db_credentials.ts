export const dbCredentials = {
  user: 'user', // e.g. 'my-db-user'
  password: 'pwd', // e.g. 'my-db-password'
  database: 'db-name', // e.g. 'my-database' - if used initialization of MariaDB found in the README then 'locpollauth'
  host: "host", // e.g. '127.0.0.1'
  port: port // e.g. 3066
};
